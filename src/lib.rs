//! Library for roster scheduling based on poll responses.
//!
//! It provides functionality for loading (`framadate`) and representing (`data`) poll responses, as well as `scheduling` based on them.

pub mod data;

pub mod framadate;

pub mod scheduling;
